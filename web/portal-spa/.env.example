# URL gốc của backend Go (labo-warranty). KHÔNG kèm /api/v1 — đã ghép trong code.
# Server-side dùng để fetch (Server Components / Server Actions).
BACKEND_URL=http://localhost:8080

# Bật standalone output khi build Docker (tuỳ chọn).
# STANDALONE=true
