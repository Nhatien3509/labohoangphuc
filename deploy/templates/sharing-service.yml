services:
  thcs-sharing-service:
    container_name: thcs-sharing-service
    image: ${IMAGE_THCS_SHARING_SERVICE:-thcs-sharing-service:latest}
    build:
      context: ../../src/services/sharing-service
      dockerfile: ../../../deploy/docker/sharing-service/Dockerfile
    restart: unless-stopped
    env_file:
      - .env.sharing-service
    ports:
      - "${APP_PORT:-8080}:8080"
    networks:
      - thcs-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

networks:
  thcs-net:
    name: thcs-net
    external: true
